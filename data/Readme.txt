These files replicate the empirical analysis in 
Rossi (2013), "Exchange Rate Predictability".

Put all the files in a directory, create a folder
called "plots" in the same directory, and within
that folder create three more directories called:
"monthly", "quarterly" and "yearly".

The main file is tables6.m


